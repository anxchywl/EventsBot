name: events_feature
description: Events tab for university super-app.
publish_to: none
version: 1.0.0+5

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  app_ui:
    path: ../app_ui
  http: ^1.2.0
  http_parser: ^4.0.0
  shared_preferences: ^2.3.0
  intl: ^0.19.0
  image_picker: ^1.1.2
  cached_network_image: ^3.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
